# net_types: STAR, RING, CHAIN, TREE, MESH, FULLY_CONNECTED, REGULAR_GRAPH, RANDOM

# range_num_nodes: [min, max] min and max number of nodes

# range_node_buffer: [min, max] range of the capacity of the nodes

# range_interfaces_per_node: [min, max] range of the number of interfaces for the nodes. Indicates the grade of the nodes.

# compromised_probability_range: the range of the probabilities for the local_sessions to get compromised. 



# protocols: HPKE, DOUBLE_RATCHET, SENDER_KEY, MLS
